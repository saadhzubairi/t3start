import SignupPage from '~/components/main/auth/signup/SignUp'; // Adjust the path as per your component's actual location

export default function SignUp() {
    return (
        <SignupPage />
    );
}